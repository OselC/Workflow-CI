name: liver_disease_env
channels:
  - conda-forge
dependencies:
  - python=3.10
  - pip
  - pip:
      - mlflow
      - dagshub
      - xgboost
      - pandas
      - scikit-learn
      - matplotlib
      - seaborn